# ВАЖНО: Всегда писать на русском языке

## Post-release TODO (после полноценного релиза MVP)
- [ ] `src/actions/creditPurchaseActions.ts`: привязать `createSbpCreditOrder/createLegalCreditOrder` к реальной user-сессии (не доверять `userId` из payload).
- [ ] `src/actions/creditPurchaseActions.ts`: привязать админ-действия (`getCreditPurchaseOrders`, `approveCreditPurchaseOrder`, `rejectCreditPurchaseOrder`) к admin-сессии (не доверять `adminUserId` из payload).
- [ ] `src/actions/creditPurchaseActions.ts`: при ошибке `expireCreditLot` не завершать отклонение заказа как `rejected` без успешного отката кредитов.
- [ ] `src/actions/templateActions.ts`: запретить мутации шаблонов “от имени другого пользователя”, сверять `userId` с текущей сессией.
- [ ] `src/actions/documentTemplateActions.ts`: админские мутации шаблонов выполнять только при подтвержденной admin-сессии.

## Статус релиза (обновлено: 1 марта 2026)
- Последний стабильный SHA в `main`: `5c54ac6`.
- GitHub Actions:
  - `CI`: success — https://github.com/FamousMonsterr/AISmetchikV9/actions/runs/22520778527
  - `Deploy VDS`: success — https://github.com/FamousMonsterr/AISmetchikV9/actions/runs/22520820135
- Инфраструктурный факт по логам:
  - приложение на основном домене живо по HTTP (`http://aismetchik.ru/api/healthz` = `200`, `Server: nginx/1.27.5`);
  - HTTPS на основном домене сейчас недоступен извне (таймаут), т.к. certbot не смог пройти challenge для поддоменов;
  - поддомены (`admin/lk/crm/partner/m`) отвечают с другого nginx (`nginx-reuseport/1.21.1`), значит DNS указывает не на текущий VDS.
- Что обязательно сделать для полного TLS:
  - выровнять DNS всех поддоменов на текущий VDS;
  - заново запустить `Deploy VDS` и проверить `https://.../api/healthz` для root + subdomains.

## Сессия 2 марта 2026 — реализация bots + CRM + subdomains + QA
- Создана ветка реализации: `feat/bots-crm-subdomains-hardening`.
- Telegram:
  - `EnvSettings` расширен полями для `user/partner/manager/admin` (token/secret/webhook/enabled).
  - Добавлены admin-actions по аудиториям:
    - `getTelegramAudienceStatus`
    - `registerTelegramWebhookByAudienceService`
    - `clearTelegramWebhookByAudienceService`
    - `pingTelegramWebhookByAudienceService`
    - `sendTelegramTestMessageByAudienceService`
    - `pingTelegramBotByAudienceService`
  - В админке Telegram добавлены вкладки аудиторий и действия register/clear/ping/test по выбранной аудитории.
  - Вынесен state/commands слой: `src/server-functions/telegram/state.ts`.
  - Webhook обработчик теперь передает `audience` в runtime (`processTelegramWebhookUpdate`).
- CRM:
  - Добавлен backend слой `src/actions/crmActions.ts`:
    - `crm_deals`, `crm_tasks`, `crm_sla_events`, `crm_activity_log`, `crm_automation_rules`
    - sync `service_requests -> crm_deals`
    - update deal/task, SLA sweep, automation rules.
  - `src/app/crm/page.tsx` переведен на полноценный workspace (Board/Table/Tasks/Timeline/SLA).
- Поддомены / infra:
  - `deploy/docker-compose.vds.yml` разделен на сервисы:
    - `web_landing`, `web_admin`, `web_lk`, `web_crm`, `web_partner`, `web_mobile`, `worker`, `nginx`.
  - nginx шаблоны обновлены на host->upstream маршрутизацию по поддоменам.
  - middleware получил `APP_SURFACE` hard-guard и 404 на чужие маршруты (без fallback на лендинг).
  - `deploy-vds.yml` health-check обновлен для всех web-сервисов.
- QA и удаление аккаунта:
  - Добавлен seed-скрипт: `scripts/qa/seed-test-user.ts` + `npm run qa:seed-user`.
  - Добавлен API self-delete: `DELETE /api/v1/auth/account` (с защитой `qaProtected`).
  - Добавлен пользовательский self-delete: `deleteOwnAccount` + кнопка “Удалить аккаунт” в профиле.
  - E2E: регистрация временного аккаунта и удаление через `v1` API.
- CI:
  - `.github/workflows/ci.yml` разбит на `unit` + `integration` + `build`, опционально `e2e` и `smoke-subdomains`.
  - Добавлены npm scripts: `test:unit`, `test:integration`, `test:smoke-subdomains`.

## Релизный протокол (GitHub + VDS)
- Основная ветка релиза: `main`.
- Перед отправкой в `main` обязательно локально выполнить:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
- После пуша в `main`:
  - проверить статус workflow `CI` в GitHub Actions;
  - проверить статус workflow `Deploy VDS`;
  - убедиться, что `GET /api/health` возвращает `ok: true`.
- Деплой-пайплайн: `.github/workflows/deploy-vds.yml`.
- Продакшен compose: `deploy/docker-compose.vds.yml`.
- Переменные деплоя на сервере: `deploy/.env.vds` + корневой `.env`.

# Логи локальной среды
- Файл `.localhost.log` может быть неотслеживаемым.
- В конце каждой сессии просматривать `.localhost.log` и кратко предлагать решения по найденным ошибкам/узким местам/оптимизациям.
- Перед новой сессией или после правок переносить текущий `.localhost.log` в `old.localhost.log` (или очищать), чтобы актуальный лог не захламлялся.

## Лог метрик `/api/db` (для прицельной оптимизации)
- Файл метрик: `.logs/api-db-metrics.jsonl`.
- Назначение: собирать telemetry по всем операциям `/api/db` (op, collection, status, duration, cache-state, shape запроса, slow-флаг).
- До отдельной команды на анализ: только накопление данных, без агрессивной очистки.
- Дата первой проверки лога: **14 февраля 2026**.
- Резервная дата проверки (если 14 февраля нет активности): **16 февраля 2026**.
- Если и 16 февраля нет достаточного объема, проверять в ближайший следующий день реального использования сервиса.

# Пайплайн анализа V1/V2 (обязательно учитывать)
- Переключатель версии пайплайна: `analysisPipelineVersion` в `AppSettings` (`src/actions/adminActions.ts`, `src/components/admin/GeneralSettings.tsx`).
- Допустимые значения:
  - `v1`: текущий классический процесс (файл -> основной анализ -> ручная проверка категорий -> отдельный шаг распределения СМР).
  - `v2`: новый серверный процесс (OCR markdown cache -> единый анализ спецификации/параметров/нестыковок/предложенных цен).
- Кэш анализа должен быть разделен по версии:
  - `file_analysis_cache` хранит `pipelineVersion` и не должен переиспользоваться между `v1` и `v2`.
- OCR markdown для `v2` хранится отдельно:
  - `file_markdown_cache` (ключ = `fileSha1`), при hit OCR-этап пропускается.
- Для файлов **не PDF** в `v1` и `v2` OCR-плагин не использовать:
  - выполнять локальный парсинг текста и изображений;
  - изображения передавать в модель как `data:image/...;base64,...`;
  - в модель отправлять извлеченный текст + список/контекст изображений.

## Серверное выполнение
- Для `v2` обработка допускается только в серверном режиме (`serverFunctionsEnabled=true` и `serverFunctionsMode='server'`).
- Основной entrypoint: `POST /api/server-analysis`.
- Исполнение пайплайна: `src/server-functions/analysis/jobRunner.ts`.
- В `v1` допустим прежний поток, но серверная задача также должна работать корректно и не ломать существующую логику.

## Провайдер AI и задел под локальную HF-модель
- Управление провайдером: `aiExecutionProvider` (`openrouter`/`local_hf`) + `localHfEnabled`.
- В `v1` анализ с файлом всегда через `openrouter`.
- В `v2` OCR-этап всегда `openrouter` (Mistral OCR), основной шаг анализа может идти через выбранный провайдер.
- Конфиг для будущего локального запуска HF:
  - `localHfBaseUrl`
  - `localHfModelId`
  - `localHfApiKey`
- По умолчанию локальная HF-модель должна быть выключена до явного включения тумблера.

## Skills по версиям
- При задачах по старому процессу использовать skill: `pipeline-v1`.
- При задачах по новому процессу использовать skill: `pipeline-v2`.
- Если версия не указана, сначала проверить `analysisPipelineVersion` в админ-настройках и только потом вносить изменения.

## Skills по OpenRouter/PDF OCR
- Для всех задач по PDF-анализу, OCR-этапу, `file-parser`, `mistral-ocr/pdf-text/native` использовать skill: `openrouter-pdf-mistral`.
- Триггеры:
  - ошибки вида `OpenRouter API Error`, `No content in OpenRouter response`, `File is too large`, `429`, `402`;
  - подозрение на неверную отправку PDF или плагина OCR;
  - настройка fallback/engine/annotations для экономии и стабильности.
- Обязательное правило: технические детали оставлять в логах, а пользователю показывать только user-friendly сообщение без сырых провайдерных body.

# Баланс / Billing UI шаблон

## Структура блока
- Две карточки в первой строке:
  - **Тариф**: заголовок, бэйдж плана, короткое описание, CTA (например, “Перейти на PRO”).
  - **Баланс**: крупное число кредитов, подпись, опционально бонусы и срок действия бонусов.
- Далее секция “Пополнить баланс” в виде сетки карточек пакетов (цена, перечень фич, выгода, CTA “Пополнить”).
- Карточка S3-хранилища: список преимуществ + CTA.
- Карточка Enterprise-предложения: градиентный фон, цена/условия, список фич, CTA “Обсудить”.
- История счетов (InvoiceHistory) и мелкий дисклеймер.

## Вёрстка и тоны
- Использовать `Card`, `CardHeader`, `CardContent`, `CardFooter`, `Badge`, `Button`.
- Грид: `grid-cols-1 lg:grid-cols-2` для верхних карточек, `md:grid-cols-2 lg:grid-cols-3` для пакетов.
- Бонусы/статусы — зелёный текст, основной план — бэйдж с акцентным цветом.
- CTA “Пополнить” на всю ширину карточки пакета, популярные помечать `border-2 border-primary`.

## Поведение
- Клики по пакету открывают диалог оплаты (PurchaseCreditsDialog) с выбранным пакетом.
- В PurchaseCreditsDialog доступна оплата по СБП (банк Сбербанк) с загрузкой чека; для юр. лиц формируется счет.
- Business/Enterprise: CTA формирует внутреннюю заявку (коллекция `service_requests`).
- Если есть бонусные кредиты и срок их действия — показывать ниже основного баланса.

## Компоненты/файлы
- Реализация: `src/app/dashboard/billing/page.tsx`.
- Диалоги: `src/components/PurchaseCreditsDialog.tsx`, `src/components/InvoiceHistory.tsx`.

Используйте эту схему как эталон для новых экранов пополнения/планов.

## Группы и синхронизация (калькулятор)
- В режиме группы есть вкладки проектов и быстрый синхро‑диалог по совпадающим названиям.
- Кнопка “Синхронизировать сейчас” открывает диалог конфликтов цен и применяет выбранные цены ко всей группе.
- В диалоге синхронизации можно применить выбранную цену либо ко всей группе, либо только к текущей вкладке.
- В группе добавление проекта вынесено в компактную вкладку “+” (стандартный анализ + списание кредита).
- ZIP‑выгрузка “вся группа” — отдельные документы на каждый проект с расширенными настройками счета.
- В выгрузке группы есть режим “Один файл”: КП с сводной страницей в начале (PDF) или сводным листом в Excel; договор — единый файл с приложениями.
- В режиме ZIP можно собрать архив по проектам или архив с набором документов (например, счет + договор).
- При включенном групповом режиме AI‑распределение цен работает сразу по всей группе как по одной спецификации; при выключенном — только по активной вкладке.
- История действий хранит 10 последних записей, включая AI‑правки и применение цен; AI‑действия сохраняются в БД и доступны после перезахода (undo тоже сохраняется).

## Групповой режим (переключатель)
- В группе добавлен тумблер “Групповая работа”: включено — действия (AI, документы, синхронизация) применяются к группе; выключено — работа только с выбранной вкладкой.
- Визуальный тон карточки группы меняется при включенном режиме (мягкий, с поддержкой светлой/темной темы), с тонкими hover/active подсветками и приглушенными цветами текста.
- Коэффициент сложности масштабирует цену монтажа каждой позиции (по текущей вкладке или по группе при активном тумблере).
- Сгруппировать проекты можно на Free, но редактирование группы доступно только PRO+ (через триал при первой попытке).
- AI-ценообразование и поиск пропущенных позиций в группе применяются ко всем сметам при включенном тумблере групповой работы.
- Добавлен блок “AI-правки проекта” с голосовым/текстовым вводом и JSON-применением изменений к смете.
- В групповом режиме AI‑правки прогоняются по всей группе (единый набор правок применяется ко всем сметам).

## AI модели и апгрейд
- Выбор модели AI доступен только на тарифах Business/Enterprise.
- На Free/PRO названия моделей скрыты: показывается “Базовая модель” / “PRO модель”. Для апгрейда доступен бейдж Business (ведет к заявке).
- На главной (Dashboard): Free — “Базовая модель”, PRO — “PRO модель”, Business/Enterprise — реальное название модели. Настройка модели — в калькуляторе.
- Для Free/PRO возможен A/B‑тест моделей: если в конфиге задано несколько моделей, пользователю показывается выбор предпочтительной (PlanModelPreference).
- Админ управляет моделями по тарифам в `src/lib/ai-config.json` → `planModels` (Free/PRO: default + abTestModels, Business/Enterprise: availableModels).
- Для Business/Enterprise нет триала: апгрейд только по заявке (service requests).
- В диалоге приватной базы цен есть кнопка подключения S3; она помечена как Business и ведет к заявке.
- Голосовой ввод в AI‑правках использует модель, выбранную как “Голос” в админ‑панели (транскрипция через AI).

## Тарифы и доступы (единые правила)
- **Free**: базовый анализ, создание групп, работа с одной сметой; без приватной базы цен и без группового редактирования.
- **PRO+ (PRO/Business/Enterprise)**: групповое редактирование, синхронизация по названию, документы группы, AI‑правки, расширенный калькулятор, приватная база цен.
- **Business/Enterprise**: все PRO + расширенные лимиты и корпоративные опции.
- В Free все PRO-функции имеют приписку “(PRO)”, в PRO+ кнопки подсвечиваются золотисто‑бронзовым акцентом.
- Для Business‑функций используются бейдж “Business” и синие акцентные стили; на Business/Enterprise эти кнопки отображаются без бейджа, но с фирменным синим тоном.
- Бейджи тарифов унифицированы через `src/components/PlanBadge.tsx`.

## Документы и формы
- КП по группе формируется в одном PDF: каждая вкладка начинается с новой страницы.
- Договор подряда для группы формирует одно тело договора и отдельные приложения по каждой вкладке (Приложение №1…N).
- Счет: поддерживает авансовый/финальный тип, основание (проект/договор), ввод номера и даты договора, настройку формулировки, опциональный акт выполненных работ.
- Для финального счета можно сформировать акт выполненных работ в составе выгрузки.
- Добавлены формы: Акт выполненных работ, КС‑2, КС‑3, КС‑6а (поддерживают групповой режим).

## Конструктор шаблонов КП
- Пользовательские шаблоны хранятся в коллекции `user_templates` (только `proposal`).
- Лимиты: PRO — 1, Business — 10, Enterprise — 50. На Free показываем “PRO функция”.
- В профиле доступно создание/редактирование/удаление шаблонов КП.
- В списках шаблонов отображаются как “(ваш)” и доступны для выбора по умолчанию.
- Параметры конструктора: стиль заголовка, цвет акцента, отображение подписи/печати.

## Навигация и чат
- Значок уведомлений расположен в навигационной панели рядом с переключателем темы (без дублей в других местах).
- Чат поддержки доступен как плавающая кнопка и открывается без перехода на отдельную страницу.
- Чат поддержки ограничен по высоте и не растягивает страницу; история прокручивается внутри блока.

## S3 бакеты по назначению
- `analysis` (по умолчанию): основной бакет из настроек S3 (`s3BucketName`) для первичного анализа и кэша.
- `avatars`: отдельный бакет для аватаров (`s3AvatarBucketName`), fallback на `s3PersonalBucketName` или основной.
- `user_docs`: документы пользователя (счета, чеки, подписи, печати) — `s3UserDocsBucketName`.
- `project_docs`: документы проектов (КП/договоры/акты) — `s3ProjectDocsBucketName`.
- Для каждого назначения можно указать свой S3 preset (`s3AvatarPresetId`, `s3UserDocsPresetId`, `s3ProjectDocsPresetId`).
- В админке: `S3 Settings` → блок “Назначение бакетов” (выбор, создание и назначение).

## Аватар
- В профиле загрузка аватара проходит через простую обрезку (crop) с выбором области.
- Компонент: `src/components/AvatarCropDialog.tsx`. Итог сохраняется как квадратный JPEG и отправляется в S3 (`avatars` bucket).

## Категории позиций (AI и UI)
- В проект добавлен отдельный тип позиции `cable_support` — **кабеленесущая конструкция**.
- Актуальный набор типов: `device`, `cable`, `cable_support`, `consumable`, `other`.
- Правила классификации:
- `device`: приборы и оборудование (прибор, датчик, устройство, сервер, камера, контроллер, считыватель и т.д.).
- `cable`: кабельная продукция (кабель/провод, витая пара, ШВВП, КПСНГ, UTP/FTP, сечение/парность).
- `cable_support`: то, в чем или по чему идет кабель (гофра, труба ПВХ/ПНД, металлорукав, кабель-канал, лоток, короб).
- `consumable`: расходные материалы и крепеж (стяжки, дюбеля, саморезы, мастика, огнезащита, перчатки и т.п.).
- `other`: все, что не удалось уверенно классифицировать.

## Коэффициент сложности (калькулятор)
- Диапазон ползунка изменен: **x0.5 .. x10.0**.
- Добавлены быстрые пресеты (кликабельные метки): по высоте монтажа и условиям доступа.
- В UI добавлен отдельный объем: **метры кабеленесущих конструкций** (как самостоятельная переменная калькулятора).

## Админ: оплаты
- Оплаты PRO: `/dashboard/admin/pro-payments`.
- Оплаты пакетов кредитов: `/dashboard/admin/credit-payments`.

## Заявки (Service Requests)
- Все “запросы по почте” заменены на внутренние заявки в коллекции `service_requests`.
- Типы: `plan_upgrade`, `s3_storage`, `crm_connector`, `estimate_department`, `partner_status`.
- Админ‑панель: `/dashboard/admin/service-requests` (статусы `new`/`in_progress`/`approved`/`rejected`).
- Клиентский хук: `src/hooks/use-service-request.ts` (единый тост + pending).
- UI‑карточка запросов: `src/components/requests/RequestFeatureCard.tsx` (framer‑motion + CSS `request-card`).

## PRO подписка (СБП + счет)
- Физ. лицо: СБП + чек (S3 `user_docs`), сразу выдается PRO на 1 день, проверка до 24 часов.
- Юр. лицо: формируется счет, хранится в `invoices`, статус `invoice_issued`.
- Авто‑апрув через 24 часа: `npm run pro:auto-approve`.

## Кредиты (ledger)
- Все начисления/списания проходят через `credit_lots` + `credit_ledger`.
- Списание: сначала бонусные, затем купленные (FIFO по дате).
- Бонусные кредиты живут 30 дней, купленные — 365 дней.
- История кредитов доступна в Billing и в админке (карточка пользователя).

## Маркетинговые бонусы
- При согласии на рассылку Free/PRO получают +10 бонусных кредитов в месяц.
- Cron: `npm run marketing:monthly-credits`.

## Dependency migration status (1 марта 2026)
- Рабочая ветка миграции: `chore/deps-node24-telegraf-exceljs`.
- Runtime обновлен до Node `24+`:
  - `.nvmrc = 24`
  - `package.json -> engines.node: >=24`
  - `Dockerfile` стадии на `node:24-alpine`
  - `.github/workflows/ci.yml` использует `node-version: 24`
- Core stack обновлен:
  - Next.js `16.1.6`
  - React/ReactDOM `19.2.x`
  - ESLint `9` (flat config `eslint.config.mjs`)
  - TypeScript `5.9.x`
- Telegram стек мигрирован:
  - удалены `node-telegram-bot-api`, `@types/node-telegram-bot-api`, `@telegram-apps/init-data-node`
  - добавлены `telegraf`, `@tma.js/init-data-node`, `@tma.js/types`, `@tma.js/transformers`
  - создан адаптер `src/lib/telegram/telegraf-compat.ts`
- Excel стек мигрирован:
  - удален `xlsx`
  - добавлен `exceljs`
  - обновлены генерация/импорт/парсинг XLSX в:
    - `src/services/excelGenerator.ts`
    - `src/services/excel/browserExcel.ts`
    - `src/server-functions/analysis/nonPdfParser.ts`
    - `src/app/dashboard/price-base/page.tsx`
- Baseline и итоговый отчет:
  - `docs/tech/deps-baseline-2026-03-01.md`
  - `docs/tech/deps-migration-report-2026-03-01.md`
- Результат security-check:
  - `npm audit --omit=dev`: `critical=0`, `high=0`.
- Локальная валидация успешна:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
- Известный технический долг после миграции:
  - Next 16 предупреждает про `middleware` (рекомендует `proxy`).
  - Предупреждения Edge-runtime из `scripts/local-log.js` (не блокируют build, но нужно вынести Node-only логгер из Edge-контекста).
